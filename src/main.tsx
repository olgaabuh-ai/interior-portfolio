import React from "react";
import ReactDOM from "react-dom/client";
import InteriorPortfolio from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <InteriorPortfolio />
  </React.StrictMode>
);
